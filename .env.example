# Frontend
VITE_API_URL=http://localhost:1337

# Strapi (example values; set real secrets in .env)
HOST=0.0.0.0
PORT=1337
APP_KEYS=changeme,changeme,changeme,changeme
API_TOKEN_SALT=changeme
ADMIN_JWT_SECRET=changeme
JWT_SECRET=changeme

# Database (PostgreSQL example)
DATABASE_CLIENT=postgres
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=koflix
DATABASE_USERNAME=koflix
DATABASE_PASSWORD=changeme
